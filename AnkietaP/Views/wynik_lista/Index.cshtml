@model IEnumerable<AnkietaP.Models.wynik_lista>

@{
    ViewBag.Title = "Index";
   
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ilosc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.opcje.nazwa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ilosc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.opcje.nazwa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_wynik_lista }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_wynik_lista }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_wynik_lista })
        </td>
    </tr>
}

</table>
