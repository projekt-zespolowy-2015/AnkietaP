@model IEnumerable<AnkietaP.Models.opcje>

@{
    ViewBag.Title = "Index";
    
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pytanie.tresc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pytanie.tresc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_opcje }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_opcje }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_opcje })
        </td>
    </tr>
}

</table>
