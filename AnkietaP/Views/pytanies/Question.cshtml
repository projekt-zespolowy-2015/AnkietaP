@model AnkietaP.Models.pytanie

@{
    ViewBag.Title = "Ankieta";
    
}
 

<div class="jumbotron" style="background-color: white">
    <div class="container">
        <div class="col-sm-4">
            <img class="img-responsive" width="300" src="~/Content/ludzik_pytanie.jpg" />
        </div>

        <div class="col-sm-8">

            @Html.DisplayFor(model => model.ankieta.opis)
            <br />
                 

                @{
                    var info = (@Html.DisplayFor(model => model.kategoria_pytania.nazwa)).ToString();
                    var info2 = "tak/nie";

                }

                @if (info.Equals(info2))
                {
                    @Html.Partial("_Question")
                }
                else
                {
                    @Html.Partial("_QuestionList")
                }


            </div>








            </div>
    </div>

</div>

